//En lugar de hacer rafc -> este es un high order componente

import { createContext } from "react";

//Definir estado inicial
const UserContext = createContext(undefined);

export default UserContext;